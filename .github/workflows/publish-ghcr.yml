name: Publish GHCR Image

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/anvilwiki

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=main,enable={{is_default_branch}}
            type=sha,prefix=git-

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            SITE_URL=${{ vars.SITE_URL || 'https://anvilwiki.pages.dev' }}
            PUBLIC_AD_MOBILE_320X50=${{ secrets.PUBLIC_AD_MOBILE_320X50 }}
            PUBLIC_AD_SIDEBAR_160X300=${{ secrets.PUBLIC_AD_SIDEBAR_160X300 }}
            PUBLIC_AD_SIDEBAR_160X600=${{ secrets.PUBLIC_AD_SIDEBAR_160X600 }}
            PUBLIC_AD_BANNER_728X90=${{ secrets.PUBLIC_AD_BANNER_728X90 }}
            PUBLIC_AD_BANNER_300X250=${{ secrets.PUBLIC_AD_BANNER_300X250 }}
            PUBLIC_AD_BANNER_468X60=${{ secrets.PUBLIC_AD_BANNER_468X60 }}
            PUBLIC_AD_NATIVE_BANNER=${{ secrets.PUBLIC_AD_NATIVE_BANNER }}
            PUBLIC_GOOGLE_ADSENSE_ID=${{ secrets.PUBLIC_GOOGLE_ADSENSE_ID }}
            PUBLIC_GA_ID=${{ secrets.PUBLIC_GA_ID }}
            PUBLIC_GSC_VERIFICATION=${{ secrets.PUBLIC_GSC_VERIFICATION }}
